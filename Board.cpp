#include "Board.h"

#define BORDER 8

Board::Board(string board)
{
	_board = new Soldier[BORDER][BORDER];

	for (int i = 0; i != BORDER; i++)
	{
		for (int j = 0; j != BORDER; j++)
		{
			_board[i][j] = 
		}
	}
}
